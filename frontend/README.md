# Frontend Workspace

This workspace is reserved for the Bun-managed React application.

The actual SPA bootstrap, routing, data fetching, and app shell are intentionally deferred to the dedicated frontend bootstrap issue so this foundation issue stays focused on monorepo setup.

