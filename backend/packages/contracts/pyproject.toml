[project]
name = "optimark-contracts"
version = "0.1.0"
description = "Shared contracts package for Optimark"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
