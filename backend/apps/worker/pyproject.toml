[project]
name = "optimark-worker"
version = "0.1.0"
description = "Background worker package for Optimark"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
